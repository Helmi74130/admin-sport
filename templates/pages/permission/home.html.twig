{% extends 'base.html.twig' %}

{% block title %}My Apps | Permission{% endblock %}

{% block body %}
    <h1 class="m-5">Gérer les permissions</h1>

        <div class="container">
            {% if not permissions.items is same as ([]) %}
            <p class="count m-0">Il y'a {{ permissions.getTotalItemCount }} permissions au total</p>
            <div class="row justify-content-around">
    {% for permission in permissions %}
                <div class="card col-5 mt-4">
                    <h5 class="card-header">{{ permission.id }}</h5>
                    <div class="card-body">
                        <h5 class="card-title">Special title treatment</h5>
                        <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
                        <a href="#" class="btn btn-primary">Go somewhere</a>
                    </div>
                </div>
    {% endfor %}
            </div>
        </div>
    <div class="navigation d-flex justify-content-center mt-5">
        {{ knp_pagination_render(permissions) }}
        {% else %}
        <h2>Il n'y a pas de permissions</h2>
        {% endif %}
    </div>
{{ dump() }}
{% endblock %}
