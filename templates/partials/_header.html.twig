<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand text-warning" href="{{ path('app_home') }}">Orange Bleu My admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Nouvelle utilisateur</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_leader') }}">Gérants</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_hall') }}">Salles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_permission') }}">Permissions</a>
                    </li>
                    <li class="nav-item">
                        <a type="button" class="nav-link btn btn-success text-light" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_user_module') }}">Mes modules</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_permission') }}">Mes permissions</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_hall_test') }}">Mes salles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_my_account') }}">Parametres</a>
                    </li>
                    <li class="nav-item">
                        <a type="button" class="nav-link btn btn-warning" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
            {#<form class="d-flex">
                <input class="form-control me-sm-2" type="text" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>#}
        </div>
    </div>
</nav>